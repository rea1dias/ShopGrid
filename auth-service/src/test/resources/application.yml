spring:
  datasource:
    url: jdbc:h2:mem:shopgrid_auth_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

security:
  jwt:
    secret: shopgrid-test-secret-key-that-is-long-enough-for-hmac-signing
    expiration: PT1H
